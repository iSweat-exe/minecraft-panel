fix(api): implement missing upload/download routes in daemon

- daemon: implement `/api/v1/files/upload` and `/api/v1/files/download` endpoints that are used by Tauri `node_client` to handle Modrinth jar downloads.
- panel: ensure `useSessions` always sends `ipv6` as `null` if undefined to prevent `422 Unprocessable Entity` parsing errors on the daemon.
